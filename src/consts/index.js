const STATUS = require('./status');
const PAGE_SITEMAP_DATA = require('./page-sitemap');
const SITEMAP_TYPE = require('./sitemap-type');

module.exports = {
  STATUS,
  PAGE_SITEMAP_DATA,
  SITEMAP_TYPE,
};
